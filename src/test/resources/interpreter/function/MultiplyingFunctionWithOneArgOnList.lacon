{
    def square(num : int) : int {
        return num * num;
    }

    values : list = [0, 1, 2, 3, 4, 5, 6, 7, 8];
    tied_fun : function = square * values;
    values = values + 9;
    result : list = tied_fun();
    println(result);
}